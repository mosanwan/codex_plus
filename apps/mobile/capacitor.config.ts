import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.codep.mobile",
  appName: "Codex+",
  webDir: "dist",
  android: {
    backgroundColor: "#ffffff"
  }
};

export default config;
