{
  "name": "@codep/mobile",
  "version": "0.1.26",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd android && ./gradlew :app:assembleDebug",
    "typecheck": "cd android && ./gradlew :app:compileDebugKotlin",
    "android:open": "printf 'Open apps/mobile/android in Android Studio.\\n'",
    "android:run": "cd android && ./gradlew :app:assembleDebug"
  }
}
